services:
    - type: web
    name: cocoon-voice-agent-6
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python implementation/server.py
    healthCheckPath: /
    autoDeploy: true